---
// Esta página es para que el ADMIN edite CUALQUIER post.
import MainLayout from '../../../../layouts/Layout.astro';
import BlogPostEditForm from '../../../../components/BlogPostEditForm.jsx';

// Astro obtiene el ID del post desde la URL.
const { id } = Astro.params;
---

<MainLayout title="Editar Post (Admin) - compassart">
  <div class="container mx-auto px-6 py-12">
    <BlogPostEditForm postId={id} client:load />
  </div>
</MainLayout>
